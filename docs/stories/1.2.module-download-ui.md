# Story 1.2: Module Download UI and User Control

## Status

Done

## Story

**As a** student,
**I want** to clearly see which modules are available to download and initiate downloads with a single button,
**so that** I can control what content is saved to my device.

## Acceptance Criteria

1. Each course module displays a prominent "Download Module" button when not yet downloaded
2. When a user clicks "Download Module", the download process begins immediately
3. During download, a progress indicator (e.g., spinner or progress bar) is visible
4. Upon successful download, the button changes to "Downloaded" or similar confirmation state
5. An error message displays if download fails, with a "Retry" option
6. The UI clearly distinguishes between "Not Downloaded", "Downloading", and "Downloaded" states

## Tasks / Subtasks

- [x] Task 1: Create course data structure with module assets (AC: 1, 2)
  - [x] Create `src/data/course.ts` file with type-safe course structure
  - [x] Define `CourseModule` type including id, title, description, and `assets` array
  - [x] Define `ModuleAsset` type with url, type (audio|pdf|image), and size fields
  - [x] Create sample module data with placeholder asset URLs for testing
  - [x] Export `getCourseModules()` function to retrieve course structure

- [x] Task 2: Set up IndexedDB for download status tracking (AC: 1, 4, 5, 6)
  - [x] Install `idb` library via npm for simplified IndexedDB transactions
  - [x] Create `src/lib/db.ts` utility file for IndexedDB operations
  - [x] Initialize IndexedDB database named "compass-db" with version 1
  - [x] Create "moduleDownloads" object store with moduleId as key
  - [x] Define download status schema: `{ moduleId: string, status: 'not-downloaded' | 'downloading' | 'downloaded' | 'error', lastUpdated: Date }`
  - [x] Implement `getModuleStatus(moduleId)` function to read status from IndexedDB
  - [x] Implement `setModuleStatus(moduleId, status)` function to update status in IndexedDB
  - [x] Add error handling for IndexedDB operations (browser support, quota exceeded)

- [x] Task 3: Create Download Icon component (AC: 1)
  - [x] Create `src/icons/download-icon.tsx` following existing icon pattern
  - [x] Use SVG download arrow icon (consistent with existing icon style)
  - [x] Accept className prop for styling flexibility
  - [x] Export as named export `DownloadIcon`

- [x] Task 4: Create ModuleDownloadButton component (AC: 1, 2, 3, 4, 5, 6)
  - [x] Create `src/components/ModuleDownloadButton.tsx` as client component
  - [x] Accept `moduleId` and `assets` array as props
  - [x] Use `useState` to manage local button state (not-downloaded, downloading, downloaded, error)
  - [x] Use `useEffect` to load initial status from IndexedDB on mount
  - [x] Implement download handler function that:
    - Communicates with service worker to cache all module assets
    - Updates local state to "downloading" immediately on click
    - Shows progress indicator during download
    - Updates IndexedDB status to "downloading" then "downloaded" on success
    - Updates state to "error" and shows "Retry" button on failure
  - [x] Render button with different UI based on status:
    - Not Downloaded: "Download Module" button with DownloadIcon
    - Downloading: Spinner/progress indicator with "Downloading..." text
    - Downloaded: "Downloaded" text with CheckmarkIcon (disabled button)
    - Error: "Retry" button with error message
  - [x] Use Tailwind classes with `clsx` for conditional styling
  - [x] Add TypeScript types for all props and state

- [x] Task 5: Enhance service worker for module asset caching (AC: 2, 3, 4)
  - [x] Update `public/sw.js` to handle custom cache messages from components
  - [x] Add message event listener for "CACHE_MODULE_ASSETS" command
  - [x] Implement module caching logic:
    - Accept array of asset URLs in message data
    - Fetch each asset and store in dedicated module cache (e.g., `module-{moduleId}-cache`)
    - Track caching progress (optional: send progress updates back to component)
    - Respond with success/failure message to component
  - [x] Add error handling for failed asset fetches (network timeout, 404 errors)
  - [x] Ensure cache-first fetch strategy applies to module assets

- [x] Task 6: Integrate ModuleDownloadButton into home page (AC: 1, 6)
  - [x] Update `src/app/(sidebar)/page.tsx` to import ModuleDownloadButton
  - [x] Update page to fetch course modules from `src/data/course.ts` (in addition to lessons)
  - [x] Render ModuleDownloadButton for each module in the PageSection
  - [x] Position button prominently below module title and description
  - [x] Ensure button styling matches overall design system (Tailwind classes)

- [x] Task 7: Test download functionality and UI states (AC: 1, 2, 3, 4, 5, 6)
  - [x] Test "Download Module" button appears for new modules (not-downloaded state)
  - [x] Test clicking download initiates caching process (downloading state)
  - [x] Test progress indicator displays during download
  - [x] Test button changes to "Downloaded" state after successful download
  - [x] Test download persists across page refreshes (IndexedDB persistence)
  - [x] Test error state by simulating network failure
  - [x] Test "Retry" button successfully re-attempts download after error
  - [x] Test multiple modules can be downloaded independently
  - [x] Verify service worker caches all module assets correctly
  - [x] Verify IndexedDB stores correct status for each module
  - [x] Run production build and test in production mode
  - [x] Format code with Prettier
  - [x] Run ESLint and fix any errors

## Dev Notes

### Previous Story Insights (from Story 1.1)

**Service Worker Implementation:**

- Service worker located at `public/sw.js` with cache-first strategy
- Cache name: `compass-v1` with versioning support
- Service worker only registers in production (via `ServiceWorkerRegistration.tsx`)
- Manual PWA implementation (no next-pwa or Serwist library due to Turbopack incompatibility)

**Project Infrastructure:**

- Next.js 15 App Router with Turbopack enabled
- TypeScript 5.8.3 strict mode
- Root layout: `src/app/layout.tsx` (already includes ServiceWorkerRegistration)
- Path aliases configured: `@/` resolves to `src/`

**Key Technical Decisions from 1.1:**

- No external PWA libraries (manual implementation preferred)
- Production-only service worker registration to avoid development conflicts
- Type-safe approach using TypeScript for all configurations

[Source: docs/stories/1.1.pwa-installation.md - Dev Agent Record]

### Architecture: Data & Offline Strategy

**Course Data Structure:**

Architecture specifies a type-safe `src/data/course.ts` file defining:

- Course structure including module titles, content types, and asset paths
- Assets (audio, PDFs) should be located in `public/` directory for static serving
- Module asset manifest: list of all files per module

**Download Process Flow:**

1. User clicks "Download" button on ModuleListItem component
2. Component logic imports course structure from `src/data/course.ts`
3. Component identifies asset URLs for that module
4. Component communicates with Service Worker via postMessage API
5. Service Worker fetches and caches specific URLs using Cache API
6. Service Worker stores assets in dedicated cache (e.g., `module-week-1-cache`)
7. Upon successful caching, component updates IndexedDB status: `not-downloaded` → `downloaded`

**Offline Serving:**

Service worker configured with cache-first strategy. When an asset is requested:

- Service Worker intercepts request
- Serves directly from Cache API if available
- Bypasses network completely for cached assets

[Source: docs/front-end-architecture.md - Section 4: Data & Offline Strategy]

### Architecture: Technology Stack

**Offline Data Storage:**

- **IndexedDB:** For storing structured data (quiz scores, download status per module, audio playback positions)
  - Use `idb` library to simplify IndexedDB transactions
- **Cache API:** For storing actual course assets (audio files, PDFs, images)
  - Managed by Service Worker

**Framework & Styling:**

- Next.js App Router with file-system-based routing
- Tailwind CSS for utility-first styling
- React 19 with TypeScript

[Source: docs/front-end-architecture.md - Section 2: Technology Stack]

### Architecture: State Management

**Component-Level State:**

For this MVP, complex global state management (Redux, Zustand) is unnecessary.

**State Strategy:**

- Use `useState` and `useEffect` for component UI state (download button state, progress)
- All persistent state (download status) read from and written to IndexedDB
- No global state library needed

[Source: docs/front-end-architecture.md - Section 5: State Management]

### Project Structure & File Locations

**Current Structure:**

```
src/
├── app/
│   ├── (sidebar)/          # Main app with navigation sidebar
│   │   ├── layout.tsx      # Sidebar layout wrapper
│   │   ├── page.tsx        # Home page (displays modules)
│   │   └── [slug]/         # Dynamic course content pages
│   ├── layout.tsx          # Root layout (includes ServiceWorkerRegistration)
│   └── offline/
│       └── page.tsx        # Offline fallback page
├── components/
│   ├── button.tsx          # Base button component (can reference for styling)
│   ├── ServiceWorkerRegistration.tsx  # SW registration (already implemented)
│   └── ... (10 other existing components)
├── data/
│   ├── lessons.ts          # Current lesson/module data
│   └── lessons/*.mdx       # MDX lesson content
├── icons/
│   ├── checkmark-icon.tsx  # Use for "Downloaded" state
│   └── ... (10 other icons, NO download icon yet)
└── lib/                    # Location for new db.ts utility
```

**New Files to Create:**

- `src/data/course.ts` - Course module data with asset URLs
- `src/lib/db.ts` - IndexedDB utility functions
- `src/icons/download-icon.tsx` - Download icon component
- `src/components/ModuleDownloadButton.tsx` - Download button component

**Files to Modify:**

- `public/sw.js` - Add module caching message handler
- `src/app/(sidebar)/page.tsx` - Integrate download buttons

[Source: docs/front-end-architecture.md - Section 3, CLAUDE.md, bash ls outputs]

### Component Styling Patterns

**Existing Component Patterns:**

Current project uses:

- Tailwind CSS utility classes for all styling
- `clsx` for conditional class composition
- Component-level state with `useState`/`useEffect`
- TypeScript strict mode for all components

**Button Styling Reference:**

From `src/components/button.tsx`:

```typescript
// Use this as reference for button styling
className =
  "rounded-full bg-gray-950 px-3 py-0.5 text-sm/7 font-semibold text-white hover:bg-gray-800 dark:bg-gray-700 dark:hover:bg-gray-600";
```

**Icon Component Pattern:**

Existing icons accept `className` prop for styling flexibility. All icon components:

- Use SVG elements
- Accept `className` prop
- Export as named exports
- Consistent sizing and stroke/fill patterns

[Source: src/components/button.tsx, src/icons/*.tsx, CLAUDE.md - Component Architecture]

### Current Data Structure (lessons.ts)

**Existing Module/Lesson Types:**

```typescript
type Module = {
  id: string;
  title: string;
  description: string;
  lessons: Lesson[];
};

type Lesson = {
  id: string;
  title: string;
  description: string;
  image: string | null;
};
```

**Note:** The new `src/data/course.ts` will need a similar structure but with added `assets` field for downloadable content. Consider aligning types or merging data structures to avoid duplication.

[Source: src/data/lessons.ts]

### Service Worker Communication Pattern

**Client to Service Worker Communication:**

Use `postMessage` API to communicate from component to service worker:

```typescript
// Send message to service worker
navigator.serviceWorker.controller?.postMessage({
  type: "CACHE_MODULE_ASSETS",
  moduleId: "module-1",
  assets: ["url1", "url2", "url3"],
});

// Listen for response from service worker
navigator.serviceWorker.addEventListener("message", (event) => {
  if (event.data.type === "CACHE_COMPLETE") {
    // Update UI to "downloaded" state
  }
});
```

**Service Worker Message Handler:**

In `public/sw.js`, add:

```javascript
self.addEventListener("message", (event) => {
  if (event.data.type === "CACHE_MODULE_ASSETS") {
    // Cache assets and respond
  }
});
```

[Source: Web API standards for Service Worker communication]

### IndexedDB Structure

**Database Schema:**

- **Database Name:** `compass-db`
- **Version:** 1
- **Object Store:** `moduleDownloads`
  - **Key:** `moduleId` (string)
  - **Value:** `{ moduleId: string, status: 'not-downloaded' | 'downloading' | 'downloaded' | 'error', lastUpdated: Date }`

**Library: idb**

Use `idb` library for promise-based IndexedDB operations (cleaner than native IndexedDB API).

Installation:

```bash
npm install idb
```

[Source: docs/front-end-architecture.md - Section 2: Technology Stack]

### Epic Technical Requirements

**Story 1.2 Technical Requirements:**

- Download button component with state management (not-downloaded, downloading, downloaded, error)
- Service Worker Cache API integration for storing module assets
- Module asset manifest (list of all files per module: audio, PDFs, images, quiz data)
- IndexedDB for storing download status per module
- Error handling for failed downloads (network timeout, storage quota exceeded)

**Dependencies:** Story 1.1 (requires service worker) - ✅ COMPLETED

**Priority:** Critical

[Source: docs/prd/epic-1-offline-content.md - Story 1.2]

### File Naming Conventions

**Code Standards:**

- React components: PascalCase (e.g., `ModuleDownloadButton.tsx`)
- Utilities/data: camelCase (e.g., `course.ts`, `db.ts`)
- TypeScript strict mode enabled
- All components and utilities must be strongly typed
- Target: ES2017

[Source: CLAUDE.md - Code Quality Standards]

### Testing

**Testing Standards:**

No formal testing strategy documentation available yet. Based on Epic requirements:

**Manual Testing Requirements:**

- Test download button states (not-downloaded, downloading, downloaded, error)
- Test download initiates caching via service worker
- Test progress indicator during download
- Test successful download updates button to "Downloaded" state
- Test download persists across page refreshes (IndexedDB)
- Test error handling and "Retry" functionality
- Test multiple modules download independently
- Verify service worker caches all assets correctly
- Verify IndexedDB stores correct status

**Automated Testing:**

- Run production build (`npm run build`)
- Verify no TypeScript errors (strict mode)
- Run Prettier to format code (`npm run format`)
- Run ESLint to check for issues (`npm run lint`)

**Test Locations:**

No specific test file location guidance available. Suggest following Next.js conventions:

- `__tests__/` directories or `.test.ts` suffixes for unit tests (future consideration)

[Source: docs/prd/epic-1-offline-content.md - Definition of Done, CLAUDE.md - Development Commands]

### Dependencies

**Story Dependencies:**

- **Story 1.1:** PWA Installation and Home Screen Access - ✅ COMPLETED
  - Service worker already implemented and functional
  - IndexedDB can now be used for persistent storage

**External Dependencies:**

- `idb` package (needs installation via npm)

[Source: docs/prd/epic-1-offline-content.md - Story 1.2 Dependencies]

### Additional Context

**Epic Goal:**

Enable students with unreliable internet access to download complete course modules (including all assets) to their devices for 100% offline access.

**User Value:**

Students can download entire course modules once and access all content (audio, readings, PDFs, quizzes) without any internet connection for the rest of the week.

**Target Users:**

- Seminary students like James in rural areas with limited/unreliable internet
- Users need to download content once when they have connectivity, then access offline for the week

**Browser Support Priorities:**

- iOS Safari (primary mobile target)
- Android Chrome (primary Android target)

[Source: docs/prd/epic-1-offline-content.md - Epic Description, Epic Goal]

### Risks & Mitigations

**Risk:** Storage quota limitations on mobile devices

**Mitigation (for this story):**

- Implement storage quota checking before downloads
- Show error message if quota exceeded
- Future stories will add ability to delete downloaded modules

**Risk:** Incomplete downloads due to poor connectivity

**Mitigation (for this story):**

- Implement retry logic via "Retry" button on error
- Future stories will add resume functionality and validation that all assets cached

[Source: docs/prd/epic-1-offline-content.md - Risks and Mitigations]

## Change Log

| Date       | Version | Description            | Author             |
| ---------- | ------- | ---------------------- | ------------------ |
| 2025-10-18 | 1.0     | Initial story creation | Bob (Scrum Master) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-5-20250929

### Debug Log References

None - no blocking issues encountered during implementation.

### Completion Notes

- Successfully implemented all acceptance criteria for module download UI and user control
- Created type-safe course data structure with module assets
- Implemented IndexedDB persistence layer using the `idb` library for download status tracking
- Created ModuleDownloadButton component with 4 states: not-downloaded, downloading, downloaded, and error
- Enhanced service worker with custom message handler for caching module assets
- Integrated download buttons into home page for all course modules
- Production build completed successfully with no TypeScript errors
- Code formatted with Prettier and ESLint checks passed (no errors in new code)
- Fixed TypeScript strict mode issue with service worker controller reference

**Implementation Highlights:**

- Used MessageChannel for bidirectional communication between component and service worker
- Implemented automatic retry functionality with error state handling
- Each module gets its own dedicated cache (e.g., `module-orientation-cache`)
- Service worker cleanup logic preserves module caches during activation
- Download status persists across page refreshes using IndexedDB

### File List

**New Files Created:**

- `src/data/course.ts` - Course module data structure with asset URLs
- `src/lib/db.ts` - IndexedDB utility functions for download status tracking
- `src/icons/download-icon.tsx` - Download icon component
- `src/components/ModuleDownloadButton.tsx` - Download button component with state management
- `public/audio/*.mp3` - 4 placeholder audio files for testing
- `public/pdfs/*.pdf` - 4 placeholder PDF workbooks for testing
- `public/images/*.png` - 4 placeholder module cover images for testing

**Modified Files:**

- `public/sw.js` - Added message event listener for module asset caching
- `src/app/(sidebar)/page.tsx` - Integrated ModuleDownloadButton into home page

**Dependencies Added:**

- `idb@8.0.3` - Promise-based IndexedDB library

## QA Results

_To be populated by QA agent_
